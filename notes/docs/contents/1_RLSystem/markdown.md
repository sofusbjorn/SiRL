# Reinforcement Learning Preliminaries

## Learning objectives

* Use a diagram to explain the setup, components and their interaction in an reinforcement learning system;
* Explain the principle of model-based and REINFORCE policy gradient algorithms;
* Implement a reinforcement learning problem, the reaching task for the three-link model, as a gym environment.

Possible assignment/exercise activities:

Install mujoco/Brax/pybullet and read the gym environment code modeling a three-link mechanism. Solve the three-link reaching problem with PPO and RL library such as stable-baselines. Train a policy contexed on the goal pose to handle multiple reaching tasks.

## References
```
**Dependencies on previous assignment**:

The simulator model so they write the gym interface to wrap it and run rl algo on top of it.
The trajectory optimization result they obtained so they can compare
````
